# EmuKC

KanColle 的服务端模拟器：实现游戏的 API 服务器，让浏览器客户端基于本地游戏数据对局。本文件是领域术语表，只定义概念，不记实现。

## Language

### 舰船成长

**Ship exp settlement（舰船经验结算）**:
把一次经验增量施加到一艘舰船上，得到新的等级、累计经验、下一级门槛和进度百分比的规则；到达 level cap 时累计经验钉在该等级的门槛值、下一级门槛与进度归零。出击、演习、远征三种来源共用同一条规则。
_Avoid_: 升级计算、exp 更新

**Level cap（等级上限）**:
舰船能达到的最高等级，由婚姻状态决定：未婚 99，已婚 175。
_Avoid_: 满级、封顶等级

**Sortie settlement（战后结算）**:
一次出击战斗结束后在同一个事务内落盘的全部写入：提督与舰船状态、海域记录、掉落舰、任务进度、后续海域解锁，以及由此得到的结果响应所需字段。出击去留（是否结束出击、stage 是否切换）不属于结算，由持有运行时状态的一方在结算提交后决定。
_Avoid_: 战斗结果处理、battleresult 逻辑

**Client view（客户端视图）**:
客户端一次请求要看到的全部状态的读取结果，例如母港、require_info、任务列表；由 gameplay 的一个视图操作按固定顺序完成前置结算与读取后一次返回，HTTP 层只把它投影到线上格式。
_Avoid_: 响应组装、handler 逻辑

**Quest observation（任务观察点）**:
领域写入完成后、事务提交前，把「刚发生了什么」交给任务模块一次的入口；它是把游戏事实翻译成任务进度的唯一位置，领域模块只描述结果，不直接推进任务。
_Avoid_: 任务事件总线、quest 钩子

### 地图与出击

**Map catalog（地图目录）**:
运行时读取的全部海域定义：每张图的变体、格子、连线、路由规则、敌方编成和掉落，格子一律用 kcdata 的编号。它由构建组装而成，不存进仓库；组装只有一个入口，顺序固定。
_Avoid_: 地图数据库、map json

**Label space（标签空间）**:
仓库里 wikiwiki 资产和掉落资产标识格子的方式：只用节点标签（A、B、Start…），不用任何格子编号。标签在组装地图目录时才一次性落到 kcdata 编号；agent 自己分配的编号在接入时就换成标签，不会进入仓库。
_Avoid_: wikiwiki 编号、BFS 编号空间

**Enemy encounter（敌方遭遇）**:
出击在一个战斗格遇到的敌人，一次选定：编成、阵形、等级、军衔和舰队名。阵形取所选编成自带的，编成没有时取该格阵形列表的首项，都没有时为単縦陣。
_Avoid_: 敌舰队生成、enemy fleet setup

### 装备

**Equipment occupancy（装备占用）**:
一件装备是否被占着：装在舰船上，或由基地航空队的中队配置着，都算占用。装上舰船、废弃、改修和改装的消耗、未装备列表都按同一个判定；已解除、正在配置转换的中队在判定前先结算，结算后不再占用装备。
_Avoid_: 已装备、equip_on 检查
